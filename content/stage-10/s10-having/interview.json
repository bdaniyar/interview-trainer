[
  {
    "question": "Что такое HAVING и как это работает?",
    "level": "normal",
    "priority": "P0",
    "interview_probability": "very_high",
    "answer": [
      "WHERE before grouping",
      "HAVING after grouping",
      "realistic order/customer examples"
    ],
    "short_answer": "`HAVING` filters groups after aggregation, while `WHERE` filters source rows before groups exist.",
    "junior_answer": "`HAVING` filters groups after aggregation, while `WHERE` filters source rows before groups exist. A condition on ordinary rows belongs in WHERE; a condition such as `COUNT(*) >= 2` belongs in HAVING. Важное ограничение: Moving a filter across aggregation can change both which rows contribute and which groups survive.",
    "follow_up_question": "Какая типичная ошибка связана с HAVING?",
    "follow_up_answer": "Writing `WHERE COUNT(*) > 1` is invalid because the aggregate has not been computed at that stage.",
    "expected_answer": {
      "must_mention": [
        "WHERE before grouping",
        "HAVING after grouping",
        "realistic order/customer examples"
      ],
      "good_additions": [
        "короткий рабочий пример",
        "ограничение или типичная ошибка"
      ],
      "common_wrong_answers": [
        "Writing `WHERE COUNT(*) > 1` is invalid because the aggregate has not been computed at that stage.",
        "Ответ без механизма, примера и ограничения"
      ],
      "red_flags": [
        "Writing `WHERE COUNT(*) > 1` is invalid because the aggregate has not been computed at that stage."
      ],
      "follow_up_questions": [
        "Какая типичная ошибка связана с HAVING?"
      ]
    },
    "follow_ups": [
      "Какая типичная ошибка связана с HAVING?"
    ],
    "tags": [
      "stage-10",
      "p0"
    ],
    "sets": [
      "stage-10",
      "full-junior-backend",
      "interview-crash-course"
    ]
  },
  {
    "question": "Какая типичная ошибка связана с HAVING?",
    "level": "follow_up",
    "priority": "P0",
    "interview_probability": "very_high",
    "answer": [
      "one short code/result example",
      "Writing `WHERE COUNT(*) > 1` is invalid because the aggregate has not been computed at that stage."
    ],
    "short_answer": "Writing `WHERE COUNT(*) > 1` is invalid because the aggregate has not been computed at that stage.",
    "junior_answer": "Writing `WHERE COUNT(*) > 1` is invalid because the aggregate has not been computed at that stage.",
    "follow_up_question": "Что такое HAVING и как это работает?",
    "follow_up_answer": "`HAVING` filters groups after aggregation, while `WHERE` filters source rows before groups exist. A condition on ordinary rows belongs in WHERE; a condition such as `COUNT(*) >= 2` belongs in HAVING. Важное ограничение: Moving a filter across aggregation can change both which rows contribute and which groups survive.",
    "expected_answer": {
      "must_mention": [
        "one short code/result example"
      ],
      "good_additions": [
        "короткий пример именно этой темы"
      ],
      "common_wrong_answers": [
        "Writing `WHERE COUNT(*) > 1` is invalid because the aggregate has not been computed at that stage."
      ],
      "red_flags": [
        "Writing `WHERE COUNT(*) > 1` is invalid because the aggregate has not been computed at that stage."
      ],
      "follow_up_questions": [
        "Что такое HAVING и как это работает?"
      ]
    },
    "follow_ups": [
      "Что такое HAVING и как это работает?"
    ],
    "tags": [
      "stage-10",
      "follow-up"
    ],
    "sets": [
      "stage-10",
      "full-junior-backend",
      "interview-crash-course"
    ]
  }
]
