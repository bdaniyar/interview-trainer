[
  {
    "question": "Что такое WHERE and boolean logic и как это работает?",
    "level": "normal",
    "priority": "P0",
    "interview_probability": "very_high",
    "answer": [
      "comparisons",
      "AND/OR/NOT",
      "parentheses",
      "ranges"
    ],
    "short_answer": "`WHERE` filters source rows using boolean predicates before grouping and aggregation.",
    "junior_answer": "`WHERE` filters source rows using boolean predicates before grouping and aggregation. AND binds tighter than OR, so parentheses make intended logic explicit. NULL comparisons yield UNKNOWN and require `IS NULL`/`IS NOT NULL`. Важное ограничение: Functions applied to an indexed column can prevent a simple index access path; confirm with EXPLAIN rather than guessing.",
    "follow_up_question": "Какая типичная ошибка связана с WHERE and boolean logic?",
    "follow_up_answer": "`status = 'paid' OR status = 'new' AND active` usually means something different from the visually assumed grouping.",
    "expected_answer": {
      "must_mention": [
        "comparisons",
        "AND/OR/NOT",
        "parentheses",
        "ranges"
      ],
      "good_additions": [
        "короткий рабочий пример",
        "ограничение или типичная ошибка"
      ],
      "common_wrong_answers": [
        "`status = 'paid' OR status = 'new' AND active` usually means something different from the visually assumed grouping.",
        "Ответ без механизма, примера и ограничения"
      ],
      "red_flags": [
        "`status = 'paid' OR status = 'new' AND active` usually means something different from the visually assumed grouping."
      ],
      "follow_up_questions": [
        "Какая типичная ошибка связана с WHERE and boolean logic?"
      ]
    },
    "follow_ups": [
      "Какая типичная ошибка связана с WHERE and boolean logic?"
    ],
    "tags": [
      "stage-10",
      "p0"
    ],
    "sets": [
      "stage-10",
      "full-junior-backend",
      "interview-crash-course"
    ]
  },
  {
    "question": "Какая типичная ошибка связана с WHERE and boolean logic?",
    "level": "follow_up",
    "priority": "P0",
    "interview_probability": "very_high",
    "answer": [
      "pattern matching",
      "`status = 'paid' OR status = 'new' AND active` usually means something different from the visually assumed grouping."
    ],
    "short_answer": "`status = 'paid' OR status = 'new' AND active` usually means something different from the visually assumed grouping.",
    "junior_answer": "`status = 'paid' OR status = 'new' AND active` usually means something different from the visually assumed grouping.",
    "follow_up_question": "Что такое WHERE and boolean logic и как это работает?",
    "follow_up_answer": "`WHERE` filters source rows using boolean predicates before grouping and aggregation. AND binds tighter than OR, so parentheses make intended logic explicit. NULL comparisons yield UNKNOWN and require `IS NULL`/`IS NOT NULL`. Важное ограничение: Functions applied to an indexed column can prevent a simple index access path; confirm with EXPLAIN rather than guessing.",
    "expected_answer": {
      "must_mention": [
        "pattern matching"
      ],
      "good_additions": [
        "короткий пример именно этой темы"
      ],
      "common_wrong_answers": [
        "`status = 'paid' OR status = 'new' AND active` usually means something different from the visually assumed grouping."
      ],
      "red_flags": [
        "`status = 'paid' OR status = 'new' AND active` usually means something different from the visually assumed grouping."
      ],
      "follow_up_questions": [
        "Что такое WHERE and boolean logic и как это работает?"
      ]
    },
    "follow_ups": [
      "Что такое WHERE and boolean logic и как это работает?"
    ],
    "tags": [
      "stage-10",
      "follow-up"
    ],
    "sets": [
      "stage-10",
      "full-junior-backend",
      "interview-crash-course"
    ]
  }
]
