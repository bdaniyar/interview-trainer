[
  {
    "question": "В чём разница между `is` и `==` и почему принято писать `x is None`?",
    "level": "normal",
    "priority": "P0",
    "interview_probability": "very_high",
    "answer": [
      "разные вопросы value/identity",
      "`__eq__`",
      "`is None`",
      "не полагаться на interning"
    ],
    "short_answer": "`==` сравнивает значения, `is` — identity объектов. С `None` используют `is`, потому что это singleton.",
    "junior_answer": "`==` проверяет равенство значений и обычно вызывает `__eq__`. `is` отвечает на другой вопрос: ссылаются ли оба имени на один объект. Поэтому два списка `[1]` равны через `==`, но обычно не идентичны. С `None` пишут `is None`: это точно выражает identity-проверку и не вызывает чужой `__eq__`.",
    "follow_up_question": "Почему нельзя проверять строки через `is`?",
    "follow_up_answer": "Python может интернировать некоторые строки, но это не гарантируется для произвольных значений; сравнение значения должно использовать `==`.",
    "expected_answer": {
      "must_mention": [
        "разные вопросы value/identity",
        "`__eq__`",
        "`is None`",
        "не полагаться на interning"
      ],
      "good_additions": [
        "короткий рабочий пример",
        "ограничение или типичная ошибка"
      ],
      "common_wrong_answers": [
        "`status is 'paid'` может случайно сработать в одном запуске и сломаться в другом; для значения нужна проверка `status == 'paid'`.",
        "Ответ без механизма, примера и ограничения"
      ],
      "red_flags": [
        "`status is 'paid'` может случайно сработать в одном запуске и сломаться в другом; для значения нужна проверка `status == 'paid'`."
      ],
      "follow_up_questions": [
        "Почему нельзя проверять строки через `is`?"
      ]
    },
    "follow_ups": [
      "Почему нельзя проверять строки через `is`?"
    ],
    "tags": [
      "stage-01",
      "p0"
    ],
    "sets": [
      "stage-01",
      "full-junior-backend",
      "interview-crash-course"
    ]
  },
  {
    "question": "Почему нельзя проверять строки через `is`?",
    "level": "follow_up",
    "priority": "P0",
    "interview_probability": "very_high",
    "answer": [
      "знать, что `id()` имеет смысл только в пределах жизни объекта",
      "Python может интернировать некоторые строки, но это не гарантируется для произвольных значений; сравнение значения должно использовать `==`."
    ],
    "short_answer": "Python может интернировать некоторые строки, но это не гарантируется для произвольных значений; сравнение значения должно использовать `==`.",
    "junior_answer": "Python может интернировать некоторые строки, но это не гарантируется для произвольных значений; сравнение значения должно использовать `==`.",
    "follow_up_question": "В чём разница между `is` и `==` и почему принято писать `x is None`?",
    "follow_up_answer": "`==` проверяет равенство значений и обычно вызывает `__eq__`. `is` отвечает на другой вопрос: ссылаются ли оба имени на один объект. Поэтому два списка `[1]` равны через `==`, но обычно не идентичны. С `None` пишут `is None`: это точно выражает identity-проверку и не вызывает чужой `__eq__`.",
    "expected_answer": {
      "must_mention": [
        "знать, что `id()` имеет смысл только в пределах жизни объекта"
      ],
      "good_additions": [
        "короткий пример именно этой темы"
      ],
      "common_wrong_answers": [
        "`status is 'paid'` может случайно сработать в одном запуске и сломаться в другом; для значения нужна проверка `status == 'paid'`.",
        "`value == None` способен вызвать переопределённый `__eq__`; точнее писать `value is None`."
      ],
      "red_flags": [
        "`status is 'paid'` может случайно сработать в одном запуске и сломаться в другом; для значения нужна проверка `status == 'paid'`."
      ],
      "follow_up_questions": [
        "В чём разница между `is` и `==` и почему принято писать `x is None`?"
      ]
    },
    "follow_ups": [
      "В чём разница между `is` и `==` и почему принято писать `x is None`?"
    ],
    "tags": [
      "stage-01",
      "follow-up"
    ],
    "sets": [
      "stage-01",
      "full-junior-backend",
      "interview-crash-course"
    ]
  }
]
