[
  {
    "question": "Что такое SQLAlchemy 2.x `select` и как это работает?",
    "level": "normal",
    "priority": "P0",
    "interview_probability": "very_high",
    "answer": [
      "`select`",
      "`where`",
      "result/scalars",
      "`.one_or_none`"
    ],
    "short_answer": "SQLAlchemy 2.x `select()` builds an explicit SQL expression executed through Session.",
    "junior_answer": "SQLAlchemy 2.x `select()` builds an explicit SQL expression executed through Session. `where` adds predicates; `session.scalars(statement)` returns the first selected entity/value column; `one_or_none` enforces at most one row while `first` merely takes one. Важное ограничение: Choose result method according to cardinality instead of silently ignoring duplicate rows.",
    "follow_up_question": "Какая типичная ошибка связана с SQLAlchemy 2.x `select`?",
    "follow_up_answer": "Using `.first()` where uniqueness is required hides duplicate-data bugs that `.one_or_none()` would expose.",
    "expected_answer": {
      "must_mention": [
        "`select`",
        "`where`",
        "result/scalars",
        "`.one_or_none`"
      ],
      "good_additions": [
        "короткий рабочий пример",
        "ограничение или типичная ошибка"
      ],
      "common_wrong_answers": [
        "Using `.first()` where uniqueness is required hides duplicate-data bugs that `.one_or_none()` would expose.",
        "Ответ без механизма, примера и ограничения"
      ],
      "red_flags": [
        "Using `.first()` where uniqueness is required hides duplicate-data bugs that `.one_or_none()` would expose."
      ],
      "follow_up_questions": [
        "Какая типичная ошибка связана с SQLAlchemy 2.x `select`?"
      ]
    },
    "follow_ups": [
      "Какая типичная ошибка связана с SQLAlchemy 2.x `select`?"
    ],
    "tags": [
      "stage-16",
      "p0"
    ],
    "sets": [
      "stage-16",
      "full-junior-backend",
      "interview-crash-course"
    ]
  },
  {
    "question": "Какая типичная ошибка связана с SQLAlchemy 2.x `select`?",
    "level": "follow_up",
    "priority": "P0",
    "interview_probability": "very_high",
    "answer": [
      "`.first`",
      "multiple rows",
      "Using `.first()` where uniqueness is required hides duplicate-data bugs that `.one_or_none()` would expose."
    ],
    "short_answer": "Using `.first()` where uniqueness is required hides duplicate-data bugs that `.one_or_none()` would expose.",
    "junior_answer": "Using `.first()` where uniqueness is required hides duplicate-data bugs that `.one_or_none()` would expose.",
    "follow_up_question": "Что такое SQLAlchemy 2.x `select` и как это работает?",
    "follow_up_answer": "SQLAlchemy 2.x `select()` builds an explicit SQL expression executed through Session. `where` adds predicates; `session.scalars(statement)` returns the first selected entity/value column; `one_or_none` enforces at most one row while `first` merely takes one. Важное ограничение: Choose result method according to cardinality instead of silently ignoring duplicate rows.",
    "expected_answer": {
      "must_mention": [
        "`.first`",
        "multiple rows"
      ],
      "good_additions": [
        "короткий пример именно этой темы"
      ],
      "common_wrong_answers": [
        "Using `.first()` where uniqueness is required hides duplicate-data bugs that `.one_or_none()` would expose."
      ],
      "red_flags": [
        "Using `.first()` where uniqueness is required hides duplicate-data bugs that `.one_or_none()` would expose."
      ],
      "follow_up_questions": [
        "Что такое SQLAlchemy 2.x `select` и как это работает?"
      ]
    },
    "follow_ups": [
      "Что такое SQLAlchemy 2.x `select` и как это работает?"
    ],
    "tags": [
      "stage-16",
      "follow-up"
    ],
    "sets": [
      "stage-16",
      "full-junior-backend",
      "interview-crash-course"
    ]
  }
]
