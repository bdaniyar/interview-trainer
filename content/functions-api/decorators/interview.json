[
  {
    "question": "Как работает decorator функции и зачем нужен `functools.wraps`?",
    "level": "normal",
    "priority": "P0",
    "interview_probability": "very_high",
    "answer": [
      "эквивалентность `@decorator` присваиванию",
      "wrapper args/result",
      "definition time",
      "`functools.wraps`"
    ],
    "short_answer": "Decorator заменяет функцию результатом `decorator(function)`; wraps сохраняет metadata и ссылку на исходную функцию.",
    "junior_answer": "Запись `@audit` выполняется при определении функции и равна `handler = audit(handler)`. Audit возвращает wrapper, который принимает arguments, вызывает исходную функцию и возвращает результат. `functools.wraps` сохраняет имя, docstring, annotations и `__wrapped__`, поэтому framework и отладчик продолжают видеть исходный контракт.",
    "follow_up_question": "В каком порядке применяются два decorators?",
    "follow_up_answer": "Применяются снизу вверх: `@a @b def f` даёт `f = a(b(f))`; при вызове внешний wrapper `a` начинает первым.",
    "expected_answer": {
      "must_mention": [
        "эквивалентность `@decorator` присваиванию",
        "wrapper args/result",
        "definition time",
        "`functools.wraps`"
      ],
      "good_additions": [
        "короткий рабочий пример",
        "ограничение или типичная ошибка"
      ],
      "common_wrong_answers": [
        "Wrapper вызывает `fn(*args, **kwargs)`, но забывает `return`, поэтому caller получает `None`.",
        "Ответ без механизма, примера и ограничения"
      ],
      "red_flags": [
        "Wrapper вызывает `fn(*args, **kwargs)`, но забывает `return`, поэтому caller получает `None`."
      ],
      "follow_up_questions": [
        "В каком порядке применяются два decorators?"
      ]
    },
    "follow_ups": [
      "В каком порядке применяются два decorators?"
    ],
    "tags": [
      "stage-03",
      "p0"
    ],
    "sets": [
      "stage-03",
      "full-junior-backend",
      "interview-crash-course"
    ]
  },
  {
    "question": "В каком порядке применяются два decorators?",
    "level": "follow_up",
    "priority": "P0",
    "interview_probability": "very_high",
    "answer": [
      "различать decorator и decorator factory",
      "порядок нескольких decorators",
      "Применяются снизу вверх: `@a @b def f` даёт `f = a(b(f))`; при вызове внешний wrapper `a` начинает первым."
    ],
    "short_answer": "Применяются снизу вверх: `@a @b def f` даёт `f = a(b(f))`; при вызове внешний wrapper `a` начинает первым.",
    "junior_answer": "Применяются снизу вверх: `@a @b def f` даёт `f = a(b(f))`; при вызове внешний wrapper `a` начинает первым.",
    "follow_up_question": "Как работает decorator функции и зачем нужен `functools.wraps`?",
    "follow_up_answer": "Запись `@audit` выполняется при определении функции и равна `handler = audit(handler)`. Audit возвращает wrapper, который принимает arguments, вызывает исходную функцию и возвращает результат. `functools.wraps` сохраняет имя, docstring, annotations и `__wrapped__`, поэтому framework и отладчик продолжают видеть исходный контракт.",
    "expected_answer": {
      "must_mention": [
        "различать decorator и decorator factory",
        "порядок нескольких decorators"
      ],
      "good_additions": [
        "короткий пример именно этой темы"
      ],
      "common_wrong_answers": [
        "Wrapper вызывает `fn(*args, **kwargs)`, но забывает `return`, поэтому caller получает `None`.",
        "Обычный sync wrapper вокруг `async def` возвращает coroutine object, но не ожидает его.",
        "Decorator скрывает signature без `@wraps(fn)`."
      ],
      "red_flags": [
        "Wrapper вызывает `fn(*args, **kwargs)`, но забывает `return`, поэтому caller получает `None`."
      ],
      "follow_up_questions": [
        "Как работает decorator функции и зачем нужен `functools.wraps`?"
      ]
    },
    "follow_ups": [
      "Как работает decorator функции и зачем нужен `functools.wraps`?"
    ],
    "tags": [
      "stage-03",
      "follow-up"
    ],
    "sets": [
      "stage-03",
      "full-junior-backend",
      "interview-crash-course"
    ]
  }
]
