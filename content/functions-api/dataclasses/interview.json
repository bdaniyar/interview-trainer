[
  {
    "question": "Что такое Dataclasses и как это работает?",
    "level": "normal",
    "priority": "P0",
    "interview_probability": "high",
    "answer": [
      "generated methods",
      "equality/repr",
      "mutable fields",
      "`field(default_factory=...)`"
    ],
    "short_answer": "`@dataclass` generates methods such as `__init__`, `__repr__` and equality from declared fields.",
    "junior_answer": "`@dataclass` generates methods such as `__init__`, `__repr__` and equality from declared fields. Fields are processed in order; `field(default_factory=list)` creates a fresh mutable default per instance. `frozen=True` blocks normal field assignment but is not deep immutability. Важное ограничение: Dataclass is good for internal data/value objects; Pydantic handles untrusted validation and ORM models handle persistence.",
    "follow_up_question": "Какая типичная ошибка связана с Dataclasses?",
    "follow_up_answer": "Using `items: list = []` is rejected/unsafe; mutable defaults need `default_factory`.",
    "expected_answer": {
      "must_mention": [
        "generated methods",
        "equality/repr",
        "mutable fields",
        "`field(default_factory=...)`"
      ],
      "good_additions": [
        "короткий рабочий пример",
        "ограничение или типичная ошибка"
      ],
      "common_wrong_answers": [
        "Using `items: list = []` is rejected/unsafe; mutable defaults need `default_factory`.",
        "Ответ без механизма, примера и ограничения"
      ],
      "red_flags": [
        "Using `items: list = []` is rejected/unsafe; mutable defaults need `default_factory`."
      ],
      "follow_up_questions": [
        "Какая типичная ошибка связана с Dataclasses?"
      ]
    },
    "follow_ups": [
      "Какая типичная ошибка связана с Dataclasses?"
    ],
    "tags": [
      "stage-05",
      "p0"
    ],
    "sets": [
      "stage-05",
      "full-junior-backend",
      "interview-crash-course"
    ]
  },
  {
    "question": "Какая типичная ошибка связана с Dataclasses?",
    "level": "follow_up",
    "priority": "P0",
    "interview_probability": "high",
    "answer": [
      "frozen",
      "domain data vs ORM/Pydantic models",
      "Using `items: list = []` is rejected/unsafe; mutable defaults need `default_factory`."
    ],
    "short_answer": "Using `items: list = []` is rejected/unsafe; mutable defaults need `default_factory`.",
    "junior_answer": "Using `items: list = []` is rejected/unsafe; mutable defaults need `default_factory`.",
    "follow_up_question": "Что такое Dataclasses и как это работает?",
    "follow_up_answer": "`@dataclass` generates methods such as `__init__`, `__repr__` and equality from declared fields. Fields are processed in order; `field(default_factory=list)` creates a fresh mutable default per instance. `frozen=True` blocks normal field assignment but is not deep immutability. Важное ограничение: Dataclass is good for internal data/value objects; Pydantic handles untrusted validation and ORM models handle persistence.",
    "expected_answer": {
      "must_mention": [
        "frozen",
        "domain data vs ORM/Pydantic models"
      ],
      "good_additions": [
        "короткий пример именно этой темы"
      ],
      "common_wrong_answers": [
        "Using `items: list = []` is rejected/unsafe; mutable defaults need `default_factory`."
      ],
      "red_flags": [
        "Using `items: list = []` is rejected/unsafe; mutable defaults need `default_factory`."
      ],
      "follow_up_questions": [
        "Что такое Dataclasses и как это работает?"
      ]
    },
    "follow_ups": [
      "Что такое Dataclasses и как это работает?"
    ],
    "tags": [
      "stage-05",
      "follow-up"
    ],
    "sets": [
      "stage-05",
      "full-junior-backend",
      "interview-crash-course"
    ]
  }
]
