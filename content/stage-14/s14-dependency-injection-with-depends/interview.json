[
  {
    "question": "Как работает `Depends` в FastAPI и каков lifecycle dependency?",
    "level": "normal",
    "priority": "P0",
    "interview_probability": "very_high",
    "answer": [
      "зачем DI",
      "dependency graph",
      "per-request cache",
      "yield cleanup"
    ],
    "short_answer": "Depends объявляет dependency graph; FastAPI разрешает его на request, кэширует одинаковые dependencies и выполняет cleanup yield-dependency.",
    "junior_answer": "`Depends` позволяет endpoint явно объявить, что ему нужны user, Session или settings. FastAPI строит graph по signatures, вызывает dependencies и передаёт результаты в handler. Внутри одного request одинаковая dependency обычно выполняется один раз. Если dependency использует `yield`, код после yield работает как cleanup. В тестах dependency можно override-нуть.",
    "follow_up_question": "Чем request cache dependency отличается от singleton?",
    "follow_up_answer": "Результат переиспользуется только внутри одного request; следующий request разрешает dependency заново. Singleton живёт между requests на уровне приложения.",
    "expected_answer": {
      "must_mention": [
        "зачем DI",
        "dependency graph",
        "per-request cache",
        "yield cleanup"
      ],
      "good_additions": [
        "короткий рабочий пример",
        "ограничение или типичная ошибка"
      ],
      "common_wrong_answers": [
        "Вызвать dependency вручную как обычную функцию и ожидать, что FastAPI разрешит её sub-dependencies.",
        "Ответ без механизма, примера и ограничения"
      ],
      "red_flags": [
        "Вызвать dependency вручную как обычную функцию и ожидать, что FastAPI разрешит её sub-dependencies."
      ],
      "follow_up_questions": [
        "Чем request cache dependency отличается от singleton?"
      ]
    },
    "follow_ups": [
      "Чем request cache dependency отличается от singleton?"
    ],
    "tags": [
      "stage-14",
      "p0"
    ],
    "sets": [
      "stage-14",
      "full-junior-backend",
      "interview-crash-course"
    ]
  },
  {
    "question": "Чем request cache dependency отличается от singleton?",
    "level": "follow_up",
    "priority": "P0",
    "interview_probability": "very_high",
    "answer": [
      "`Annotated` aliases",
      "`use_cache=False`",
      "граница dependency/service",
      "Результат переиспользуется только внутри одного request; следующий request разрешает dependency заново. Singleton живёт между requests на уровне приложения."
    ],
    "short_answer": "Результат переиспользуется только внутри одного request; следующий request разрешает dependency заново. Singleton живёт между requests на уровне приложения.",
    "junior_answer": "Результат переиспользуется только внутри одного request; следующий request разрешает dependency заново. Singleton живёт между requests на уровне приложения.",
    "follow_up_question": "Как работает `Depends` в FastAPI и каков lifecycle dependency?",
    "follow_up_answer": "`Depends` позволяет endpoint явно объявить, что ему нужны user, Session или settings. FastAPI строит graph по signatures, вызывает dependencies и передаёт результаты в handler. Внутри одного request одинаковая dependency обычно выполняется один раз. Если dependency использует `yield`, код после yield работает как cleanup. В тестах dependency можно override-нуть.",
    "expected_answer": {
      "must_mention": [
        "`Annotated` aliases",
        "`use_cache=False`",
        "граница dependency/service"
      ],
      "good_additions": [
        "короткий пример именно этой темы"
      ],
      "common_wrong_answers": [
        "Вызвать dependency вручную как обычную функцию и ожидать, что FastAPI разрешит её sub-dependencies.",
        "Создать глобальную SQLAlchemy Session и возвращать её всем requests.",
        "Поместить всю бизнес-логику в огромную dependency graph, которую трудно тестировать отдельно."
      ],
      "red_flags": [
        "Вызвать dependency вручную как обычную функцию и ожидать, что FastAPI разрешит её sub-dependencies."
      ],
      "follow_up_questions": [
        "Как работает `Depends` в FastAPI и каков lifecycle dependency?"
      ]
    },
    "follow_ups": [
      "Как работает `Depends` в FastAPI и каков lifecycle dependency?"
    ],
    "tags": [
      "stage-14",
      "follow-up"
    ],
    "sets": [
      "stage-14",
      "full-junior-backend",
      "interview-crash-course"
    ]
  }
]
